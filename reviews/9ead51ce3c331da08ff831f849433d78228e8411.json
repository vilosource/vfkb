{
  "recordVersion": 1,
  "sha": "9ead51ce3c331da08ff831f849433d78228e8411",
  "governing": ["docs/adr/ADR-0070-guards-that-can-fail.md", "docs/adr/ADR-0052-review-gate.md"],
  "reviewer": { "agent": "general-purpose", "model": "opus", "rounds": 2 },
  "generated": "2026-07-26T13:35:13Z",
  "findings": [
    { "id": "F1", "severity": "blocking", "status": "fixed", "summary": "the git-compound hook wedged when stdin never closed (issue #214 class, wired to every Bash call) — measured never-exiting at 5s while the sibling hook exits at 2s. Fixed with the sibling's settle-on-deadline pattern; the watchdog DECIDES ON WHAT ARRIVED, so a dangerous payload is still denied. Observed: terminated=false red without the watchdog, terminated+denied green with it.", "acceptedBy": null },
    { "id": "F2", "severity": "major", "status": "fixed", "summary": "newline is a command separator and the regex treated it as prose — the false-negative hole covered the routine multi-line Bash shape, and the comment's claimed heredoc residual did not exist for the same reason. \\n added to both separator classes; 4 newline-chain deny cases observed red pre-fix.", "acceptedBy": null },
    { "id": "F3", "severity": "major", "status": "fixed", "summary": "git -C <dir> checkout && tail defeated the anchor (subcommand required immediately after git). Global-flag skip added, deny case observed both ways.", "acceptedBy": null },
    { "id": "F4", "severity": "minor", "status": "fixed", "summary": "mutations:[null] threw a TypeError instead of producing a finding; null-entry guard + selftest case (63/63).", "acceptedBy": null },
    { "id": "F5", "severity": "minor", "status": "accepted", "summary": "env/time/VAR=-prefixed and quoted branch switches pass the anchor — documented as accepted residuals in the hook header; a shell parser, not a regex, would be needed. Deliberate: the hook is a guardrail, not a jail.", "acceptedBy": "vilosource" },
    { "id": "F6", "severity": "minor", "status": "fixed", "summary": "the PR body claimed 'hook selftest red 5/5 under a gutted deny'; the shipped selftest carried 4 deny cases. Post-fix tally: 8 deny cases, 10 FAILs observed under a gutted deny. Corrected here per ADR-0070's own corollary.", "acceptedBy": null }
  ],
  "findingsCount": 6,
  "ruledOut": [],
  "mutations": [
    { "guard": "scripts/review-gate.selftest.mjs > ADR-0070 cases (no array / empty-no-note / observedRed!==true)", "mutation": "neutralized the bad.push calls in the mutations validation block", "observedRed": true },
    { "guard": "scripts/review-gate.selftest.mjs > a null mutations entry is a finding, not a crash", "mutation": "reviewer probe: mutations:[null] against the pre-fix gate threw TypeError", "observedRed": true },
    { "guard": "scripts/hook-git-compound-guard.selftest.mjs > 8 deny cases", "mutation": "gutted the deny branch (if(false)) — 10 FAILs", "observedRed": true },
    { "guard": "scripts/hook-git-compound-guard.selftest.mjs > newline-chain deny cases", "mutation": "removed \\n from both separator classes — 4 FAILs", "observedRed": true },
    { "guard": "scripts/hook-git-compound-guard.selftest.mjs > heredoc-prose allow cases", "mutation": "the unanchored \\b regex (observed live: it blocked the rule's own documentation, then its own fix)", "observedRed": true },
    { "guard": "scripts/hook-git-compound-guard.selftest.mjs > [watchdog] stdin held open", "mutation": "removed the setTimeout watchdog — terminated=false observed", "observedRed": true }
  ],
  "verdict": "MERGE"
}
